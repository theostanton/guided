{
  "name": "@guided/deploy",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy:staging": "./package.sh && terraform workspace select staging && terraform apply -var-file vars/staging.tfvars",
    "deploy:production": "terraform workspace select production && terraform apply -var-file vars/production.tfvars"
  }
}