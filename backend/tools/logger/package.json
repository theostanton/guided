{
  "name": "@guided/logger",
  "version": "0.0.0",
  "private": true,
  "main": "./srv/index.js",
  "types": "./srv/index.d.ts",
  "scripts": {
    "prepare": "yarn build",
    "build": "yarn install && tsc",
    "test": "jest",
    "watch": "tsc -w"
  },
  "dependencies": {
    "chalk": "^3.0.0"
  }
}