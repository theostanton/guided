import memoize from "../../../utils/memoize";
// import {DayRow} from "../../../database/types";

// const dao = memoize<DaysDao>(() => {
//     return new DaysDao()
// });
//
// function day(_: void, {id}: { id: string }): Promise<DayRow | null> {
//     return dao().get(id)
// }
//
// function allDays(_: void, args: void): Promise<DayRow[]> {
//     return dao().getAll()
// }

export {}