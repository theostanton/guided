{
  "name": "@guided/compute",
  "version": "0.0.0",
  "private": true,
  "main": "./srv/index.js",
  "types": "./srv/index.d.ts",
  "scripts": {
    "build:pack": "webpack",
    "build:build": "tsc",
    "build:zip": "zip -r ../../../deploy/dist/compute.zip dist",
    "dist": "yarn build:build && yarn build:pack",
    "prepare": "yarn build",
    "build": "tsc",
    "test": "jest",
    "watch": "tsc -w",
    "invoke": "STAGE=staging STAGE_ID=guide_ps03v4w ts-node src/invoke.ts"
  },
  "dependencies": {
    "aws-sdk": "^2.619.0",
    "@guided/utils": "*",
    "@guided/google": "*",
    "@guided/database": "*",
    "@guided/spinup": "*",
    "@guided/geojson": "*",
    "date-fns": "^2.9.0",
    "@googlemaps/google-maps-services-js": "^2.0.2"
  },
  "devDependencies": {
    "serverless": "^1.63.0",
    "jest": "^25.1.0",
    "@types/jest": "^25.1.2",
    "ts-jest": "^25.2.0",
    "ts-node": "^8.6.2",
    "faker": "^4.1.0",
    "@types/faker": "^4.1.0"
  },
  "workspaces": {
    "nohoist": [
      "**"
    ]
  }
}
