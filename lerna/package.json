{
  "name": "guided",
  "private": true,
  "version": "0.0.1",
  "devDependencies": {
    "lerna": "^3.19.0",
    "lerna-update-wizard": "^0.17.7"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "clean": "rm -rf node_modules && lerna clean -y",
    "deploy": "lerna run deploy && ./deploy.sh",
    "start:api:prod": "cd backend/api && yarn start:prod",
    "develop:website": "lerna run develop --parallel --ignore usersite",
    "develop:usersite": "lerna run develop --parallel --ignore website"
  }
}
