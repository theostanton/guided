version: 2.1

orbs:
  aws-cli: circleci/aws-cli@0.1.19
  terraform: ngs/terraform@0.0.1

jobs:
  deploy-frontend:
    docker:
      - image: circleci/node:12.9.1
    working_directory: ~/workspace
    steps:
      - checkout
      - restore_cache:
          key: frontend
      - run:
          name: "Install"
          command: |
            cd frontend/website
            yarn install
      - save_cache:
          paths:
            - frontend/website/node_modules
          key: frontend-{{ checksum "yarn.lock" }}

  deploy-backend:
    docker:
      - image: circleci/node:12.9.1
    working_directory: ~/workspace
    steps:
      - checkout
      - add_ssh_keys
#      - restore_cache:
#          key: backend
      - run:
          name: "Install terraform"
          command: |
            cd deploy
            sudo  sh ./install_terraform.sh
      - run:
          name: "Install"
          command: |
            cd backend
            yarn install
#      - save_cache:
#          paths:
#            - backend/node_modules
#          key: backend-{{ checksum "yarn.lock" }}
      - run:
          name: "Init"
          command: |
            cd deploy
            terraform init
      - run:
          name: "Build"
          command: |
            cd deploy
            yarn build:backend:staging
      - run:
          name: "Deploy"
          no_output_timeout: 30m
          command: |
            cd deploy
            terraform workspace select staging
            terraform apply -var macro_version=0 -var private_key_path=/home/circleci/.ssh/id_rsa_5eab1ebe72710affd8434c2c6e2f82e0 -auto-approve


workflows:
  version: 2
  Deploy Staging:
    jobs:
      - deploy-backend:
          context: guided-staging
          filters:
            branches:
              only: staging
      - deploy-frontend:
          requires:
            - deploy-backend
